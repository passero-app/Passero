name: Build & Release

on:
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.gate.outputs.should_run }}
    steps:
      - id: gate
        run: |
          if [[ "${{ github.event.head_commit.author.name }}" == "github-actions[bot]" ]]; then
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          fi

  version:
    needs: check
    if: needs.check.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.calver.outputs.version }}
      tag: ${{ steps.calver.outputs.tag }}
    steps:
      - id: calver
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          YY=$(date -u +%y)
          MM=$(date -u +%m)
          BASE="${YY}.${MM}"

          # Find next available build number for this month
          N=1
          while gh release view "v${BASE}.${N}" >/dev/null 2>&1; do
            N=$((N + 1))
          done
          VERSION="${BASE}.${N}"

          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Will create release v${VERSION}"

  build:
    needs: version
    if: needs.version.outputs.tag != ''
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            label: macOS-arm64
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            label: Linux-x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            label: Windows-x64

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.label }})

    steps:
      - uses: actions/checkout@v4

      - name: Patch version
        shell: bash
        run: |
          VERSION="${{ needs.version.outputs.version }}"
          sed -i.bak 's/"version": "0.0.0"/"version": "'"${VERSION}"'"/' package.json
          sed -i.bak 's/^version = "0.0.0"/version = "'"${VERSION}"'"/' src-tauri/Cargo.toml
          sed -i.bak 's/"version": "0.0.0"/"version": "'"${VERSION}"'"/' src-tauri/tauri.conf.json
          find . -name '*.bak' -delete

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Update Cargo.lock
        run: cargo generate-lockfile --manifest-path src-tauri/Cargo.toml

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --target ${{ matrix.target }}
          tagName: ${{ needs.version.outputs.tag }}
          releaseName: Passero ${{ needs.version.outputs.tag }}
          releaseBody: |
            Download the installer for your platform below.

            | Platform | File |
            |----------|------|
            | macOS (Apple Silicon) | `.dmg` |
            | Linux | `.deb` / `.AppImage` |
            | Windows | `.msi` / `.exe` |
          releaseDraft: false
          prerelease: false

  update-cask:
    needs: [version, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download release DMG and compute checksum
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ needs.version.outputs.version }}"
          TAG="${{ needs.version.outputs.tag }}"
          BASE="https://github.com/${{ github.repository }}/releases/download/${TAG}"

          curl -fL -o arm64.dmg "${BASE}/Passero_${VERSION}_aarch64.dmg"
          ARM64_SHA=$(sha256sum arm64.dmg | awk '{print $1}')

          sed -i "s/version \"[^\"]*\"/version \"${VERSION}\"/"                       Casks/passero.rb
          sed -i "s/sha256 \"[^\"]*\" # :arm64/sha256 \"${ARM64_SHA}\" # :arm64/"     Casks/passero.rb

      - name: Commit updated cask
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/passero.rb
          if ! git diff --cached --quiet; then
            git commit -m "cask: bump to ${{ needs.version.outputs.tag }}"
            git pull --rebase origin main
            git push
          fi
